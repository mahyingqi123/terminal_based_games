package game.enemies;

/**
 * Enumeration class that specifies the type where actor cannot attack to prevent multi string literals being used
 * @author Ying Mah
 * @version 1.0
 */
public enum CannotAttack {
    FISH,
    DOG,
    UNDEAD,
    STORMVEIL,
    INVADER,
    ALLY
}
